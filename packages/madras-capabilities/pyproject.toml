[project]
name = "madras-capabilities"
version = "0.0.1"
description = "Madras AI — the Capability Catalog (Plans/code-restructure.md A5)."
requires-python = ">=3.11,<3.12"
dependencies = [
  "pydantic>=2.7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/madras_capabilities"]
