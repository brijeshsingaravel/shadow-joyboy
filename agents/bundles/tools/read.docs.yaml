name: read.docs
description: Read-only access to local documentation files.
rank_required: intern
scopes: [read]
mcp_servers:
  - mcp_filesystem
credential_policy:
  issuance: jit_task_scoped
  max_ttl_seconds: 600
