# Shadow — subagents (frozen-contract)

**Declared — none configured yet.** Delegation is built at the engine level (`delegate_team` /
`delegate_parallel`: Magentic-One dual-ledger + per-worker tool scoping + governed synthesizer). Per-agent
subagent specs (`*.yaml`) are authored in **W3/W5** of the rebuild (constellation routing · arbiter agents ·
supervisor conditional-gate); this directory is the conformance target they fill.
